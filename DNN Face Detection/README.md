# Face Detection using OpenCV DNN

This project implements real-time face detection using OpenCV's DNN module and a pre-trained SSD model.

## Model Used
- SSD (Single Shot Detector)
- Backbone: ResNet-10
- Framework: Caffe
- Input size: 300×300

## Requirements
- Python 3.11+
- OpenCV
- NumPy

Install dependencies:
```bash
pip install opencv-python numpy
